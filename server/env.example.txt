# Server environment variables (copy to .env)

# Web server
PORT=5000

# MongoDB connection
# Example (local): mongodb://localhost:27017/expense_tracker
# Example (Mongo Atlas): mongodb+srv://<user>:<pass>@cluster0.xxxxx.mongodb.net/expense_tracker
MONGO_URI=

# JWT secret for signing auth tokens
JWT_SECRET=change_me_dev_secret

# Allowed frontend origins (comma-separated). Example for dev and Vercel:
# CLIENT_URLS=http://localhost:5173,https://expense-tracker-alpha-hazel-19.vercel.app
CLIENT_URLS=

# Single client origin (legacy support). If both are set, CLIENT_URLS takes precedence
CLIENT_URL=
